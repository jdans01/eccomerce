{%- liquid
  assign hero_image = section.settings.image
  if hero_image == blank and section.settings.product != blank
    assign hero_image = section.settings.product.featured_image
  endif
-%}
<div class="hero section-padding{% if hero_image == blank %} hero--plain{% endif %}" style="{% if section.settings.min_height %}min-height:{{ section.settings.min_height }}px;{% endif %}">
  {%- if hero_image -%}
    <div class="hero__media">
      <img
        src="{{ hero_image | image_url: width: 2000 }}"
        srcset="{{ hero_image | image_url: width: 800 }} 800w, {{ hero_image | image_url: width: 1400 }} 1400w, {{ hero_image | image_url: width: 2000 }} 2000w"
        sizes="100vw"
        alt="{{ hero_image.alt | escape }}"
        width="{{ hero_image.width }}"
        height="{{ hero_image.height }}"
        loading="eager"
      >
    </div>
    <div class="hero__scrim"></div>
  {%- else -%}
    <div class="hero__glow hero__glow--1" aria-hidden="true"></div>
    <div class="hero__glow hero__glow--2" aria-hidden="true"></div>
  {%- endif -%}

  <div class="hero__content page-width">
    {%- if section.settings.eyebrow != blank -%}
      <span class="eyebrow">{{ section.settings.eyebrow }}</span>
    {%- endif -%}
    {%- if section.settings.heading != blank -%}
      <h1>{{ section.settings.heading }}</h1>
    {%- endif -%}
    {%- if section.settings.text != blank -%}
      <p>{{ section.settings.text }}</p>
    {%- endif -%}
    <div class="hero__actions">
      {%- if section.settings.button_label != blank -%}
        <a href="{{ section.settings.button_link | default: '#' }}" class="btn btn--primary">{{ section.settings.button_label }}</a>
      {%- endif -%}
      {%- if section.settings.button_label_2 != blank -%}
        <a href="{{ section.settings.button_link_2 | default: '#' }}" class="btn {% if hero_image %}btn--outline-light{% else %}btn--secondary{% endif %}">{{ section.settings.button_label_2 }}</a>
      {%- endif -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Hero",
  "settings": [
    { "type": "image_picker", "id": "image", "label": "Imagen de fondo" },
    { "type": "product", "id": "product", "label": "Producto (usa su foto si no eliges una imagen arriba)" },
    { "type": "range", "id": "min_height", "label": "Altura mínima", "min": 400, "max": 800, "step": 20, "unit": "px", "default": 640 },
    { "type": "text", "id": "eyebrow", "label": "Texto superior", "default": "Nueva colección" },
    { "type": "text", "id": "heading", "label": "Título", "default": "Diseño consciente para tu día a día" },
    { "type": "textarea", "id": "text", "label": "Texto", "default": "Piezas atemporales hechas con materiales de calidad, pensadas para durar." },
    { "type": "text", "id": "button_label", "label": "Botón principal", "default": "Comprar ahora" },
    { "type": "url", "id": "button_link", "label": "Enlace del botón principal" },
    { "type": "text", "id": "button_label_2", "label": "Botón secundario", "default": "Ver colecciones" },
    { "type": "url", "id": "button_link_2", "label": "Enlace del botón secundario" }
  ],
  "presets": [
    { "name": "Hero" }
  ]
}
{% endschema %}
